# W3 D5 — 2026-05-25

## 4a. Session header

- **Date:** 2026-05-25
- **Week:** 3 — Sliding window
- **Day:** 5 (final)
- **Role:** Retrospectivist
- **Estimated time:** ~30 min
- **Problems attempted:** 0 new; acceptance test re-confirmed LC#121 and LC#3

---

## 4b. Problems attempted

### LC#121 — Best Time to Buy and Sell Stock (acceptance test only)

No new code written. Re-ran D2/D3 solution on LeetCode.

- **Verdict:** Accepted — 212/212 (0ms, 100% C++ runtime, 86.8% memory, 97.3 MB)
- **Complexity (from prior sessions):** O(n) time, O(1) space. Each element visited once; one variable tracks running min buy price; running max profit updated each step.

### LC#3 — Longest Substring Without Repeating Characters (acceptance test only)

No new code written. Re-ran D2 hashset solution on LeetCode.

- **Verdict:** Accepted — 988/988 (7ms, 64.34% C++ runtime, 56.2% memory, 11.9 MB)
- **Complexity (from prior sessions):** O(n) time, O(n) space. Each character added to and removed from the set at most once; dominant term is n.

---

## 4c. Conceptual wins

- **Set-mirrors-window invariant** [FIRST TIME] — "The window invariant is that the set mirrors the window exactly (every window char is tracked in the set), so s[l] by definition is in the set. set.contains(s[l]) is always true while window is non-empty which causes the loop to only stop when set is empty past duplicate." Four sessions at 1.5 before this cold retrieval. The specific phrase "set mirrors window exactly" was the missing piece in every prior session — now explicitly stated and causally connected.
- **std::max semantics in LC#121** — "it makes sure that max is updated to the running max." Correct and unprompted when probed. (The D4 issue was naming a variable `max`, which shadows `std::max` and causes a compile error — a naming habit to break, not a semantic gap.)

---

## 4d. Scaffolding log

- **Retrospective self-assessment:** User said "everything was a struggle." Pushed back: named three specific wins from the week (LC#15 inner-skip score 2 — first time, D3; LC#3 O(n) derivation — first time, D2; s[l] invariant closed today). User accepted the correction. Not a DSA scaffolding event — retrospective calibration only.
- **std::max gap claim:** User listed std::max as a conceptual gap. When asked what it does in LC#121, answered correctly immediately. The actual D4 issue was variable shadowing (variable named `max` shadowing `std::max`) — a naming habit, not semantic confusion. No scaffolding required.

Overall DSA scaffolding: **none.** D5 was acceptance test + retrospective only.

---

## 4e. Retrieval scores

| Prompt | Score | Notes |
|--------|-------|-------|
| W3 P1 open — In LC#3's hashset version, why does `while (set.contains(s[l]))` over-shrink? What property guarantees `set.contains(s[l])` is always true while window is non-empty? | **2 [FIRST TIME]** | "Set mirrors window exactly" → "s[l] by definition in set" → "always true while non-empty" — complete causal chain, cold, no scaffolding. Four sessions to close. |
| W3 P1 close (Step 6) | **2** | Same answer as open — "set mirrors window exactly" → causal chain complete. Consistent at close; real durability test is W4 D1 open. |

---

## 4f. Momentum and blockers

W3 complete and on pace. Both Accepted, no logic bugs in any solution. The critical retrieval item (s[l] set-mirrors-window invariant) closed at D5 after four sessions — normal difficulty for a subtle invariant. Main carry-forward entering W4: `static_cast<int>(s.size())` has recurred three times across three weeks without becoming automatic; W4 D4 must surface it without prompting. Secondary: W3 P3 (LC#3 std::max backward-jump, score 1) was not retested this session — resurface at W4 D3. Binary search (W4) introduces loop invariant reasoning (`l ≤ r` vs `l < r`) as a new class of off-by-one; user's self-identified process change (trace on paper before code) is exactly the right preparation and will be tested on D2. LC#76 Minimum Window Substring (Hard) was not attempted; deferred.

---

## 4g. Next session contract

- **First action:** W4 D1 — Binary search pattern intro from first principles. Before the session, re-read the YTM bisection solver from cpp-finance-zero-to-hero to activate the curriculum overlap (same algorithm, different domain). No keyboard until loop invariant (`l ≤ r` vs `l < r`) and mid calculation are traced on paper.
- **Retrieval question:** In LC#3's hashset version, why does `while (set.contains(s[l]))` over-shrink the window? What property of the set guarantees `set.contains(s[l])` is always true while the window is non-empty? (Spaced W3 P1 — first clean close was today; confirm it persists at W4 D1 open.)
- **Carry-forwards:** `static_cast<int>(s.size())` must appear without prompting in W4 D4; LC#76 Minimum Window Substring (Hard) deferred — resurface as spaced problem; W3 P3 (std::max backward-jump, score 1) not retested — resurface W4 D3.

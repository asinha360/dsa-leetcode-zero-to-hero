# W3 Day 4 — 2026-05-24

## 4a. Session Header

- **Date:** 2026-05-24
- **Week:** 3 — Sliding Window
- **Day:** 4
- **Role:** Code Reviewer (Senior Engineer)
- **Estimated time:** ~1.0h
- **Problems attempted:** 0 new (code review session for previously-Accepted LC#3 and LC#121)
- **Problems solved:** 0 new

---

## 4b. Problems Reviewed

### LC#3 — Longest Substring Without Repeating Characters

- **Pattern:** Sliding window + hashmap (O(1) jump optimization over hashset shrink loop)
- **Complexity stated before review:** O(n) time ("each character added/removed from set once"), O(n) space. Derivations correct. Claude noted tighter space bound: O(min(n, |alphabet|)); O(n) accepted as upper bound.
- **LeetCode verdict:** Previously Accepted 988/988, 4ms (73rd percentile). No resubmission this session — fixes applied but verdict carry-forward to D5 acceptance test.
- **Issues found in review:**
  1. **BLOCKING** — `while (r < s.size())`: signed/unsigned comparison. `r` is `int`, `s.size()` is `size_t`. Fixed: `r < static_cast<int>(s.size())`. **Recurring issue — 3rd occurrence** (W1 D4: `i < nums.size()`; LC#3 today; LC#121 today). Must become reflex.
  2. **BLOCKING** — variable name `map` shadows `std::map` from standard library. Fixed: renamed to `char_pos`.
  3. **NIT** — `char_pos.count(s[r])` works (returns 0 or 1 for unordered_map) but `char_pos.contains(s[r])` is cleaner C++20. Noted, not changed.
- **Code review question — `std::max(l, char_pos[s[r]] + 1)`:** User initially confused this with LC#121 ("keeps track of latest max profit"). After being given the "abba" trace prompt (r=3, l=2, map['a']=0 → l would jump to 1), user correctly identified: "sends l back — movement in the wrong direction." Concept confirmed with trace.

### LC#121 — Best Time to Buy and Sell Stock

- **Pattern:** Sliding window (single-pass — l = min buy day, r scans forward)
- **Complexity stated before review:** O(n) time ("all ints iterated through"), O(1) space ("no extra space except var max"). Both correct. Derivation for time needs one more word: "iterate once" → O(n).
- **LeetCode verdict:** Previously Accepted 212/212, 0ms. No resubmission this session — fixes applied, verdict carry-forward to D5 acceptance test.
- **Issues found in review:**
  1. **BLOCKING** — `while (r < prices.size())`: same signed/unsigned issue. Fixed: `r < static_cast<int>(prices.size())`.
  2. **BLOCKING** — `int max` shadows `std::max`. Fixed: renamed to `max_profit`. (Code used `std::max(max, ...)` which worked due to qualification, but variable name was confusing.)
- **No logic bugs.** Algorithm is correct. Invariant: l always points to minimum price seen so far; l advances only when a cheaper buy is found.

---

## 4c. Conceptual Wins

- **`set.contains(s[l])` over-shrink mechanism** — after explanation, stated correctly: "s[l] is window invariant — always in set while window non-empty; loop only stops when empty, eliminating innocent chars." [Needed scaffolding — not a cold win]
- **`set.contains(s[r])` stops at right place** — stated correctly: "stops once specific collision resolved, i.e., duplicate no longer in set/window." [Needed scaffolding]
- **`std::max` backward-jump prevention (LC#3)** — after trace, correctly identified: "sends l back — movement in the wrong direction." Score 1 (needed redirect + trace).
- **LC#11 pointer direction invariant** — after one nudge ("what determines the height?"), stated: "height determined by shorter pointer — moving taller can only decrease height." Score 2 at close.
- **STL fluency: `.count` / `.find` / `.contains`** — all three correctly described. Minor imprecision: said `.find()` "returns bool I think" (corrected: `.find()` returns iterator; the `!= map.end()` comparison returns bool).

---

## 4d. Scaffolding Log

**1. LC#3 s[l] over-shrink mechanism (W3 P1 carry-forward)**
- User stated: "no sliding window — single pointer systematically eliminating chars from a set." Correct observation but missing the WHY.
- Wrong framing: did not state the window invariant ("every window character is in the set, therefore s[l] is always in the set").
- Also said "c is not in the set at that moment" (incorrect — at check 3, set = {c}).
- Correction given: full explanation — "window invariant: set mirrors the window exactly; s[l] is always in the set while the window is non-empty; therefore the condition is always true; loop over-shrinks to empty." Concrete trace provided: [a,b,c], s[r]='b' → l ends at 3 (wrong) not 2 (correct).
- Also explained why `set.contains(s[r])` terminates correctly: "s[r] is outside the window; it's in the set only because there's a duplicate inside; once we remove that duplicate, the condition becomes false — at exactly the right moment."
- Did it land? Yes — user paraphrased both sides correctly at end of explanation.

**2. `std::max(l, char_pos[s[r]] + 1)` purpose (LC#3 code review)**
- User confused this line with LC#121: "keeps updated track of latest max profit — without it sliding window may not update to new latest max."
- Wrong because: this is LC#3, not LC#121; line 71 controls the left pointer, not a profit tracker.
- Redirect: "Trace `s = 'abba'` at r=3. l=2. map['a']=0. What does `l = map['a'] + 1` give you?"
- After trace, user correctly said: "sends l back by 1 step — movement in the wrong direction."
- Did it land? Partial — got backward-jump direction correct; full invariant ("only move l forward, never backward") stated by Claude.

**3. `.find()` return type**
- User said: `.find()` "returns bool I think."
- Wrong: `.find()` returns an iterator. The expression `find() != end()` returns bool.
- Correction given: stated explicitly; not re-tested.

---

## 4e. Retrieval Scores

| Prompt | Opening score | Closing score | Notes |
|--------|--------------|--------------|-------|
| W3 P1 — LC#3 s[l] over-shrink: why does `set.contains(s[l])` over-shrink while `set.contains(s[r])` stops correctly? | 1.5 | [closing check — see Step 6] | Traced first two loop steps correctly; missed window invariant WHY; needed full explanation |
| W1 spaced — LC#11: why move shorter pointer, not taller? | 1.5 (open) | 2 (close) | Opened: correct conclusion, missing mechanism ("moving taller leads to smaller area"). After nudge: "height determined by shorter pointer — moving taller can only decrease height." |

---

## 4f. Momentum and Blockers

On pace for W3. Both code reviews complete; solutions are now clean C++. The recurring `static_cast<int>(s.size())` issue is a muscle-memory gap — it has appeared in every review session (W1 D4, LC#3, LC#121). It is not a conceptual misunderstanding; it is a habit that needs to become automatic. The primary conceptual blocker is W3 P1: the window invariant that makes `set.contains(s[l])` always true. This required a full explanation today — it is not cold-owned. D5 acceptance test is lightweight (both problems previously Accepted). The real risk is the D5 opening retrieval: if W3 P1 is not cold-owned by then, code review of the hashset variant has no foundation.

---

## 4g. Next Session's Contract

- **First action:** Open cold, no notes: "Why does `while (set.contains(s[l]))` over-shrink the window? State the window invariant and what it means for the loop condition." Must score 2 before acceptance test begins.
- **Retrieval question:** "In LC#3's hashset version, why does `while (set.contains(s[l])) { set.erase(s[l]); l++; }` over-shrink the window instead of stopping at the duplicate? What property of the set guarantees `set.contains(s[l])` is always true?"
- **Carry-forwards:** W3 P1 s[l] over-shrink must close cold (three sessions at 1.5); `static_cast<int>(s.size())` must be applied without prompting — check in D5 acceptance test solutions; LC#121 and LC#3 acceptance test re-run to confirm fixed solutions still pass.

# W3 D3 — Retrieval Practice (2026-05-23)

## Session header

- **Date:** 2026-05-23
- **Week 3, Day 3** — Pattern: Sliding Window
- **Role:** Algorithm Tutor (retrieval practice mode)
- **Time estimate:** ~0.5 hours

---

## Retrieval scores

| Prompt | Score | Notes |
|--------|-------|-------|
| LC#3 hashset — exact while condition + why `s[l]` breaks it | 1.5 | Condition correct and clean (improvement from D2 where it took 2 redirects). "Why s[l] breaks" directionally correct (s[l] is the wrong thing to check) but mechanism wrong: said "stalls" when actual behavior is the opposite — s[l] is always in the set, loop over-shrinks / runs away. |
| LC#15 inner-skip — condition + why while loop | 2 `[FIRST TIME]` | Both conditions correct (`nums[l]==nums[l-1]`, `nums[r]==nums[r+1]`). Why while loop: "many duplicates could exist adjacent — keep moving until next unique element" — captures the missing insight from D2 ("after one step you may still be on same-value element"). Four sessions at 1.5; first clean retrieval. |
| LC#3 hashmap — why `std::max` guards backward jump | 1 | Got there with two redirects. Condition: "it gives 1 which is inaccurate — should be updated to 2 instead which max ensures" — identified the concrete wrong value. Did not spontaneously name the invariant violation (window would contain duplicate). Needed redirect: "what's inside the window when l=1?" → self-stated "b is duplicated." |

---

## LC#121 confirmation

- **Verdict:** Accepted, 212/212 (0 ms, beats 100% runtime, 59.97% memory)
- **Carry-forward closed:** yes.
- **Complexity confirmed:** Time O(n) (user-derived, correct), Space O(1) (user-derived, correct).

---

## Code flags for D4 review

- `int max` on line 61 of LC#121 solution: variable name shadows `std::max` called on line 67.
- `r < prices.size()`: signed/unsigned comparison (`int` vs `size_t`). Same flag as W1 D4 — recurring.

---

## Carry-forwards to D4

- LC#3: why `s[l]` over-shrinks (invariant framing: s[l] is always in the set, condition never naturally false) — mechanism not cleanly retrieved; reinforce at D4 open.
- LC#121: `int max` shadowing + signed/unsigned flag — review in code review.

---

## Next session contract (D4)

- **Role:** Code Reviewer (Senior Engineer)
- **First action:** Code review LC#121, LC#3, LC#76 (if attempted). User derives complexity first.
- **STL fluency check:** `.find` vs `.contains` vs `.count` — when to use each.
- **Retrieval at open:** LC#3 why `s[l]` over-shrinks (one more pass on the invariant).

---

## Project Architect addendum (2026-05-23, post-retrieval)

User switched to Project Architect role to enforce CLAUDE.md section 12 (session files written only on /end). Prior behavior: Claude was writing PROGRESS.md, TASKS.md, and notes/ mid-session despite the existing prohibition.

**Decision made:** Hookify block rule (Option B) — harness-level enforcement, not model compliance.

**Changes:**
- Created `.claude/hookify.block-session-files.local.md` — PreToolUse block rule on PROGRESS.md, TASKS.md, DECISIONS.md, notes/wX_dY.md. Active immediately; no restart needed.
- Updated `.claude/commands/end.md` — added Step 0 (disable guard via `sed`) and Step 8 (re-enable guard after /sync).
- Saved feedback memory at `~/.claude/projects/.../memory/feedback_session_file_writes.md`.

**Bypass mechanism:** /end disables the rule (Step 0), writes all files, then re-enables it (Step 8). Hookify re-reads rules dynamically on each tool use, so the toggle takes effect immediately.

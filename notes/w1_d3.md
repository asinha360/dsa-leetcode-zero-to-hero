# W1 Day 3 — Retrieval Practice + LC#15 3Sum

**Date:** 2026-05-14
**Time budget:** ~0.5h retrieval + ~1.5h LC#15 attempt

---

## Retrieval scores (W1, closed-book)

| Prompt | Score | Notes |
|--------|-------|-------|
| Exit condition `while (l < r)` — why correct for odd and even length | 2 | Clean. Key: l == r means same element, can't form a pair. |
| LC#11 — why move the shorter pointer, not the taller | 2 | Initially inverted; self-corrected. "Moving taller pointer only decreases area — width shrinks, height cap unchanged." |
| Concrete unsorted example where two-pointer fails | 2 | `[1,3,4,2]`, target 4 — pair (1,3) skipped. Monotonicity breaks. |

---

## LC#15 — 3Sum [H] — Accepted (316/316)

**Pattern:** Sort + outer fixed-element loop + inner two-pointer
**Target complexity:** O(n²) time, O(log n) auxiliary space

### Algorithm (plain English)
1. Sort the array — restores monotonicity so two-pointer can eliminate.
2. Outer loop: fix `nums[i]`. Skip if `nums[i] == nums[i-1]` (duplicate fixed element → duplicate triplets).
3. Inner two-pointer on subarray `[i+1 … n-1]`. Target = `-nums[i]`.
   - Sum too large → `r--`
   - Sum too small → `l++`
   - Sum exact → push triplet, `l++; r--`, then skip duplicate `l` and `r` values.

### Complexity derivation
- Sort: O(n log n)
- Outer loop: n iterations
- Inner loop: each pointer moves at most n times per outer iteration → O(n) per iteration
- Total: O(n) × O(n) = O(n²), dominates sort
- Auxiliary space: O(log n) for `std::sort` introsort stack

### Accepted solution
```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        std::sort(nums.begin(), nums.end());
        vector<vector<int>> triplets {};

        for (int i = 0; i < nums.size(); i++) {
            if (i > 0 && nums[i] == nums[i-1]) continue;

            int target = -(nums[i]); int l = i + 1; int r = nums.size() - 1;

            while (l < r) {
                if (nums[l] + nums[r] > target) {
                    r--;
                } else if (nums[l] + nums[r] < target) {
                    l++;
                } else {
                    triplets.push_back({nums[i], nums[l], nums[r]});
                    l++; r--;
                    while (l < r && nums[l] == nums[l-1]) l++;
                    while (l < r && nums[r] == nums[r+1]) r--;
                }
            }
        }
        return triplets;
    }
};
```

### Key insight built today
After sorting, fixing one element reduces 3Sum to 2Sum on a sorted subarray — a problem already solved. The hard part is duplicate suppression at three independent points: outer loop, l pointer, r pointer.

### Struggles
- Initially tried range-based for loop — can't get index for l = i+1
- Duplicate check condition took multiple iterations to get right (was checking `nums[l] == x` which rejects valid triplets like [-1,-1,2])
- `||` vs `&&` in duplicate skip while loops

---

## Carry-forwards
- None. W1 Day 4 (Code Reviewer) is next.

---

## Full session record (appended at /end)

### 4a. Session header
- **Date:** 2026-05-14
- **Week / Day:** W1 / Day 3
- **Role:** Algorithm Tutor (retrieval practice mode)
- **Time:** ~2h (0.5h retrieval + ~1.5h LC#15)
- **Problems attempted:** 1 | **Problems solved (Accepted):** 1

### 4b. Problems attempted

#### LC#15 — 3Sum [H]
- **Pattern identified:** Sort + outer fixed-element loop + inner two-pointer (stated before coding after Socratic prompting)
- **Target complexity stated:** Initially O(n) / O(1) — revised to O(n²) time / O(log n) space after reading problem and reasoning through the outer loop
- **Approach:** Sort array to restore monotonicity. Fix one element per outer loop iteration. Run two-pointer on the remaining subarray to find pairs summing to `-nums[i]`. Three duplicate-suppression points: outer loop (`i > 0 && nums[i] == nums[i-1]`), after finding a triplet on l (`nums[l] == nums[l-1]`), and on r (`nums[r] == nums[r+1]`).
- **Key insight:** After sorting and fixing `nums[i]`, the problem reduces to 2Sum on a sorted subarray — self-discovered through Socratic questioning ("if the first element is fixed, what problem remains?").
- **Hints given:**
  - Rung 0 (clarification, ~10 min in): "Can I sort the array first?" — answered yes with rationale (triplets not indices, so ordering is safe).
  - Rung 1 (~30 min in, stuck on inner loop structure): "After sorting, if you fix one element, what problem are you now solving with the remaining elements?" — user answered "target sum = 4" and connected to LC#11.
- **LeetCode verdict:** Accepted — 316/316, 42ms, beats 87.13% C++ runtime.
- **Final complexity:** O(n²) time (outer n × inner n), O(log n) auxiliary space (`std::sort` introsort stack). User derived O(n²) correctly. Space prompted — user said O(log n) after one question about what `std::sort` costs.
- **Code quality notes:**
  - Used range-based for loop initially — corrected to index-based when l = i+1 required the index.
  - Duplicate check condition wrong twice: first `nums[l] == x` (rejects valid triplets like [-1,-1,2]), then moved inside else but pushed after incrementing l (pushed wrong value). Correct form is compare adjacent elements after advancing.
  - `||` vs `&&` in duplicate skip while loops — caught and corrected.
  - `push_back([...])` square-bracket syntax — corrected to `{}`.
  - `std::sort(nums)` → `std::sort(nums.begin(), nums.end())` — corrected.
  - Final solution is clean and idiomatic.

### 4c. Conceptual wins
- **3Sum reduces to 2Sum after fixing one element** — "A target sum that adds up to 4" — self-stated after one Socratic prompt. `[FIRST TIME]`
- **Duplicate suppression location** — "they would have to be the same [on the next iteration]" — correctly identified that duplicates come from consecutive equal values, not from comparing to the fixed element. `[FIRST TIME]`
- **O(n²) derivation** — "outer loop n times × inner loop n times" — clean, unprompted.
- **`std::sort` auxiliary space is O(log n)** — retrieved correctly on one question.

### 4d. Scaffolding log
- **`std::vector<vector<int>>` syntax** — user tried `std::vector<int> triplets = {std::vector<int> x, ...}` and `vector<vector<vector<int>>>`. Corrected by asking "a triplet is three ints — what type is that? a collection of those — what type is that?" Landed correctly after two exchanges.
- **Duplicate check condition** — user believed `nums[l] == x` was the right guard. Corrected by tracing `[-1,-1,2]` by hand: `nums[l] == x == -1` fires, valid triplet rejected. User understood immediately.
- **`||` vs `&&` in skip loops** — user wrote `while (l < r || nums[l] == nums[l-1])`. Corrected by explaining: `||` runs the loop even when `l >= r` (out of bounds). Landed on first explanation.

### 4e. Retrieval scores
| Prompt | Score | Notes |
|--------|-------|-------|
| Exit condition `while (l < r)` — odd/even | 2 | Clean. Added: l == r = same element, can't form pair. |
| LC#11 — why move shorter pointer | 2 | Initially inverted; self-corrected before prompted. |
| Unsorted array where two-pointer fails | 2 | `[1,3,4,2]` target 4 — pair (1,3) skipped. Concrete. |

### 4f. Momentum and blockers
User is ahead of pace for W1 D3 — solved the Hard problem (LC#15) on the first attempt with only one Socratic hint and one clarification. The two-pointer pattern is solidifying well. The primary weakness is syntax under novel conditions: when the data structure or loop form is unfamiliar, the user reaches for incorrect syntax (range-based for when index is needed, wrong initializer syntax, `||` vs `&&`). This is expected at 5 weeks of C++ experience and will improve with reps. The conceptual model is strong — duplicate suppression, complexity derivation, and the reduction insight were all self-derived. Most likely blocker for Day 4 (Code Review): being asked to derive complexity cold for all three W1 solutions without notes. The space complexity for LC#15 (O(log n) auxiliary) is the one most likely to need a nudge.

### 4g. Next session's contract
- **First action:** W1 Day 4 — Code Reviewer mode. Open all three solution files (`lc15_three_sum.cpp`, `125.valid-palindrome.cpp`, `11.container-with-most-water.cpp`). Derive time and space complexity for each before Claude reviews any of them.
- **Retrieval question:** After sorting, why does fixing one element of a triplet reduce 3Sum to a problem you already know — and what are the three places duplicate suppression must fire?
- **Carry-forwards:** none
